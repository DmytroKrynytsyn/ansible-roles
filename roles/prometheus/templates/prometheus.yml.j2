global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "node"
    static_configs:
      - targets:
{% for target in nodes_to_srcape %}
          - "{{ target }}:9100"
{% endfor %}